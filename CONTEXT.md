# dsh-skins 个性化

DSH Web 皮肤插件的个性化语境：四套皮肤（meirenzhi / openbmc / uefi-harness / tgcf）共用的声明式外观定制体系，以及承载它的交互容器与编辑语义。

## Language

### 皮肤

**官方皮肤**:
宿主自带的默认外观（DeepSeek Harness），可在外观选择中选择，但不进入个性化目录，没有可编辑字段。
_Avoid_: 默认皮肤

**出厂皮肤**:
未持久化任何皮肤选择时的兜底皮肤，注册顺序第一位。用户显式选择（含选官方）后立即让位。
_Avoid_: 默认皮肤（与官方皮肤混淆）

### 交互容器

**皮肤切换弹层**:
换肤按钮唤出的浮层，承载外观选择与皮肤列表。
_Avoid_: 设置弹窗、一级菜单

**个性化面板**:
齿轮展开的编辑区，与皮肤切换弹层粘连展示；窄视口时上下堆叠。
_Avoid_: 设置页、二级菜单

**粘连外壳**:
弹层与面板构成的单一浮层整体，共享同一套关闭逻辑（点空白处、再点换肤按钮、Esc）。

### 编辑语义

**自动保存**:
面板内的字段变更经短暂防抖自动写入配置存储并跨标签页同步；无保存操作。
_Avoid_: 手动保存、草稿

**恢复默认**:
把全部字段重置为出厂默认；重置前列出非默认设置并需二次确认，确认后立即生效并自动保存。
_Avoid_: 还原（已废除）

### 字段

**壁纸**:
皮肤背景图，取自内置精选或图库。

**内置精选**:
皮肤自带的可选壁纸，不可删除。
_Avoid_: 默认壁纸

**通透度**:
会话面板等容器的通透程度；单值联动驱动面板底色、遮罩与模糊三个视觉层，0% 即纯壁纸完全可见。
_Avoid_: 面板不透明度、面板透明度（旧称）

**标语**:
新会话页的引导语，中英双语各一份。

### 资产与存储

**图库**:
服务端保存的上传图片集合，跨皮肤共享；增删即时生效，不走保存制。
_Avoid_: 相册、壁纸库

**清空图库**:
删除图库全部图片；所有引用它的壁纸字段回退默认，需逐项确认影响。

**快照**:
配置存储的全量当前状态，含图库与各皮肤覆写。

**修订号**:
配置的乐观锁计数，写入必须基于最新修订号，落后即冲突。
_Avoid_: 版本号（与配置格式版本混淆）

**存量规范化**:
启动加载配置时，按当前字段目录剔除未知键与校验不通过的覆写值，删除已卸载皮肤的整段，并落盘。
_Avoid_: 数据迁移、清理脚本（它是加载路径的内置步骤，不是独立工具）
