# dsh-skins 个性化

DSH Web 皮肤插件的个性化语境：三套皮肤（openbmc / uefi-harness / tgcf）共用的声明式外观定制体系，以及承载它的交互容器与编辑语义。

## Language

### 交互容器

**皮肤切换弹层**:
换肤按钮唤出的浮层，承载外观选择与皮肤列表。
_Avoid_: 设置弹窗、一级菜单

**个性化面板**:
齿轮展开的编辑区，与皮肤切换弹层粘连展示；窄视口时上下堆叠。
_Avoid_: 设置页、二级菜单

**粘连外壳**:
弹层与面板构成的单一浮层整体，共享同一套关闭逻辑（点空白处、再点换肤按钮、Esc）。

### 编辑语义

**预览**:
已在本地界面生效、尚未保存到配置存储的修改。
_Avoid_: 草稿、临时修改

**保存**:
把预览显式写入配置存储；此后跨标签页可见并持久化。
_Avoid_: 应用、生效（与预览的本地生效混淆）

**还原**:
丢弃全部预览，回到上次已同步的值。
_Avoid_: 取消、重置

**恢复默认**:
把字段值重置为出厂默认；产生的仍是预览，待保存。
_Avoid_: 还原（方向相反）

**未保存修改**:
预览层非空的状态；离开粘连外壳需经确认。

### 字段

**壁纸**:
皮肤背景图，取自内置精选或图库。

**内置精选**:
皮肤自带的可选壁纸，不可删除。
_Avoid_: 默认壁纸

**遮罩**:
压在壁纸上的暗色层强度；单值，亮暗主题同值。

**模糊**:
壁纸的高斯模糊半径。

**面板透明度**:
会话面板等容器的通透程度。

**标语**:
新会话页的引导语，中英双语各一份。

### 资产与存储

**图库**:
服务端保存的上传图片集合，跨皮肤共享；增删即时生效，不走保存制。
_Avoid_: 相册、壁纸库

**清空图库**:
删除图库全部图片；所有引用它的壁纸字段回退默认，需逐项确认影响。

**快照**:
配置存储的全量当前状态，含图库与各皮肤覆写。

**修订号**:
配置的乐观锁计数，写入必须基于最新修订号，落后即冲突。
_Avoid_: 版本号（与配置格式版本混淆）

**存量规范化**:
启动加载配置时，按当前字段目录剔除未知键与校验不通过的覆写值，删除已卸载皮肤的整段，并落盘。
_Avoid_: 数据迁移、清理脚本（它是加载路径的内置步骤，不是独立工具）
